# Release process

Release inputs and policy are tracked in `release/policy.json`. The
project ships source distributions, headless binaries, and a desktop
shell artefact; signed binaries require the build infrastructure
described in `release/policy.json`.

Detailed rollback, migration, and operations documentation is expected
to grow alongside the first public release. This file is the
placeholder the release-policy validator points at.